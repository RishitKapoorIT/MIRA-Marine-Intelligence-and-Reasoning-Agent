# Single backend origin. The frontend talks only to the ORCA backend.
# VITE_AUTH_API_BASE and VITE_PFZ_API_BASE are retired: services/auth-api is
# removed, and PFZ is served from the backend's cache endpoint, never by
# calling services/pfz-api (:8001) directly.
VITE_API_BASE=http://localhost:8000